// import { CSS, Button, styled } from '@nextui-org/react';
// import { useState } from 'react';
// import { json } from 'stream/consumers';

// export const MyCustomButton = styled(Button, {
//     padding: '$2 $4',
//     background: '$purple400',
//     margin: '$2 $4',
// });

// // Use `CSS` or the configured type as shown above
// export type MyButtonProps = { css?: CSS };

// const MyButton = (props: MyButtonProps) => {
//     const [val, setVal] = useState(1);

//     return (
//         <MyCustomButton {...props} onClick={() => setVal(val+1)}>
//             {val}
//         </MyCustomButton>
//     );
// };

// export default MyButton;